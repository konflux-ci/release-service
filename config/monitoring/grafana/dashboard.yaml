apiVersion: integreatly.org/v1alpha1
kind: GrafanaDashboard
metadata:
  name: main-dashboard
  labels:
    app: appstudio-grafana
spec:
  configMapRef:
    name: grafana-dashboard-release-release-service
    key: release_service.json
